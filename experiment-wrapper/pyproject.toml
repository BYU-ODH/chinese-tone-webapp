[project]
name = "tone-tiger-experiment"
version = "0.1.0"
description = "Experiment wrapper around the Chinese tone trainer"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2",
]

[dependency-groups]
dev = []
